---
import "../styles/global.css";
import Schema, {l10n} from "../components/Schema.astro";
import { buildHreflangNetwork } from "../lib/hreflang";


// Достаем пропсы, которые передаются в Layout (например, из index.astro)
const { lang, title = "North Dakota Fairytales" , description} = Astro.props;


const currentL10n = (l10n && l10n[lang]) ? l10n[lang] : l10n.en;

// 2. Создаем единую переменную для описания (Пункт 11)
const siteDesc = description || currentL10n.desc;
const currentLang = lang || 'en';

const hreflangNetwork = buildHreflangNetwork(Astro.site, Astro.url.pathname);
---

<!DOCTYPE html>
<html lang={currentLang}>
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="google-site-verification" content="5-9qk07xItWbNdlo2_YNTB3O4mzPEJcI4B8Zfw7vFtg" />

    <title>North Dakota Fairytales | Choose Language</title>

    <meta name="description" content={siteDesc} />
    <meta property="og:description" content={siteDesc} />
    <meta name="twitter:description" content={siteDesc} />

    <meta property="og:title" content="North Dakota Fairytales" />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="https://northdakotafairytales.org/" />
    <meta property="og:image" content="https://northdakotafairytales.org/preview.jpg" />

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content="North Dakota Fairytales" />
    <meta name="twitter:image" content="https://northdakotafairytales.org/preview.jpg" />

    <link rel="canonical" href={`${Astro.site}${Astro.url.pathname}`} />

    <link rel="alternate" hreflang="x-default" href={hreflangNetwork.xDefault} />
    {hreflangNetwork.alternates.map(({ hreflang, href }) => (
        <link rel="alternate" hreflang={hreflang} href={href} />
    ))}

    <Schema
            title={title}
            lang={currentLang}
            description="Choose your language to explore calm winter stories and songs for children from North Dakota Fairytales."
    />

    <!--<title>{title}</title>-->
</head>
<body>

<div id="snow"></div>

<slot />

<script define:vars={{ "currentLang" : currentLang }}>

    // так как мы явно передали данные из Astro в браузерный JS.

    const snowContainer = document.getElementById("snow");

    function createSnowflake() {
        if (!snowContainer) return;

        const snowflake = document.createElement("div");
        snowflake.classList.add("snowflake");

        // Используем переменную, пришедшую с сервера
        snowflake.textContent = "❄️";

        // Стилизация снежинки
        snowflake.style.left = Math.random() * 100 + "vw";
        snowflake.style.fontSize = (Math.random() * 20 + 14) + "px";
        snowflake.style.animationDuration = (Math.random() * 8 + 8) + "s";
        snowflake.style.opacity = (Math.random() * 0.7 + 0.3).toString();

        snowContainer.appendChild(snowflake);

        // Удаление через 16 секунд, чтобы не перегружать память браузера
        setTimeout(() => {
            snowflake.remove();
        }, 16000);
    }

    // Запуск генерации снежинок
    setInterval(createSnowflake, 180);
</script>

<style is:global>
    /* Базовые стили для работы снега */
    #snow {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        pointer-events: none; /* Чтобы снег не мешал нажимать на кнопки */
        z-index: 9999;
        overflow: hidden;
    }

    .snowflake {
        position: absolute;
        top: -20px;
        user-select: none;
        animation: fall linear forwards;
    }

    @keyframes fall {
        to {
            transform: translateY(110vh);
        }
    }
</style>
</body>
</html>
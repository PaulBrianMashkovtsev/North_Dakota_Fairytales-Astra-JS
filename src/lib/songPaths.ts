/**
 * Canonical URLs for song pages (content collection + recommendations under Old Snow Music).
 */
export function oldSnowSongHref(lang: string, slug: string): string {
    return `/${lang}/old-snow-music/songs/${slug}`;
}
